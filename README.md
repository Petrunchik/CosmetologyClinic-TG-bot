# CosmetologyClinic-TG-bot

Телеграм-бот для клиники косметологии — инструмент, который помогает пользователям оставить заявку для обратной связи, связаться с менеджером и подписаться на информационные рассылки клиники.

## 🚀 Возможности

- Оставление заявки для обратной связи 
- Просмотр информационной рассылки
- Интерактивное меню с командами  
- Хранение данных о клиентах в базе данных
- Отправка рассылки всем пользователям
- Функции для отслеживания статистики бота
- Логирование активности бота  

## 📂 Структура проекта
   ```bash
   .
   ├── .env                   # Переменные окружения (токен, конфигурация БД и др.)
   ├── alembic.ini            # Конфигурация Alembic для миграций БД
   ├── app/                    # Прикладные функции бота
   ├── bot/                    # Основная логика работы
   ├── database/               # Работа с БД
   ├── logs/                   # Папка для логов
   ├── migrations/             # Файлы миграций БД
   ├── settings/               # Конфигурация приложения
   ├── main_clinic_bot.py      # Точка входа бота
   └── req.txt                 # Зависимости
   ```
## ⚙️ Установка и запуск

1. Клонируйте репозиторий:

   ```bash
   git clone https://github.com/Petrunchik/CosmetologyClinic-TG-bot.git
   cd CosmetologyClinic-TG-bot
   ```

 2. Создайте виртуальное окружение и установите зависимости:

      ```bash
      python3 -m venv venv
      source venv/bin/activate  # на Linux / macOS
      # или `venv\Scripts\activate` на Windows
      pip install -r req.txt
      ```

 3. Настройте файлы .env и main_clinic_bot.py с необходимыми переменными:
      ```bash
      
      BOT_TOKEN=ваш_токен_бота - .env
      
      chat_id=id_чата, для уведомления о работе бота - main_clinic_bot.py
      ```

 4. Выполните миграции базы данных:
      ```bash
      alembic upgrade head
      ```


 5. Запустите бота:
      ```bash
      python main_clinic_bot.py
      ```


## 🛠 Использованные технологии

Проект построен с использованием современного и надёжного стека:

### Backend / Telegram
- Aiogram — асинхронный Python-фреймворк для Telegram-ботов  
- Python 3.10+

### База данных и миграции
- SQLAlchemy — ORM для работы с БД  
- Alembic — система миграций  
- SQLite - легковесная файловая БД

### Инфраструктура и прочее
- pydantic — валидизация и конфигурация  
- Logging встроенный — для записи логов  
- Структурированная архитектура (handlers, services, repositories)


### 📚 Логирование
- Все логи сохраняются в папку logs/.
- Формат и уровень логирования можно настроить через конфигурационные файлы в settings/.
